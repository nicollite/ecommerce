import { HideResizeDirective } from "./hide-resize.directive";

describe("HideResizeDirective", () => {
  it("should create an instance", () => {
    const directive = new HideResizeDirective();

    expect(directive).toBeTruthy();
  });
});
